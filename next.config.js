module.exports = {
  env: {
    API_URL: process.env.API_URL,
  },
//   webpack: (config, { isServer }) => {
//     if (!isServer) {
//       config.node = {
//         fs: `empty`,
//       };
//     }
//     return config;
//   },
};
